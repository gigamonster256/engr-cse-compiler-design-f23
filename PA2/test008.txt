// Chris Baldwin 
// 525003366
// Tests not, relative ops, and call inside relation

main

{
    // Check not handling
    if(not(not(1==0))) then
        printInt(-3);
    else
        printInt(3);
    fi;
    // Check rel operators
    if(-1 < 1) then 
        printInt(4);
    else
        printInt(-4);
    fi;
    if(1 >= call readInt()) then
        printInt(5);
    else
        printInt(-5);
    fi;
}