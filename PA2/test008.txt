// Chris Baldwin 
// 525003366
// Tests and, or, relative ops

main

{
    if(2 and 1) then
        printInt(-1);
    else 
        printInt(1);
    fi;
    if(-1 or 1) then
        printInt(2);
    else
        printInt(-2);
    fi;
    if(not(not(1==0))) then
        printInt(-3);
    else
        printInt(3);
    fi;
    if(-1 < 1) then 
        printInt(4);
    else
        printInt(-4);
    fi;
    if(1 >= call readInt()) then
        printInt(5);
    else
        printInt(-5);
    fi;
}